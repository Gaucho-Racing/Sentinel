export declare type UpdaterFn<TStore> = (state: () => Partial<TStore>) => void;
export declare type Reducer<TStore, TAction> = (store: TStore, action: TAction) => TStore;
export declare type Listener<TStore> = {
    selectorFn: (store: TStore) => TStore;
    updater: UpdaterFn<TStore>;
};
export declare type SetStoreAction<TStore> = TStore | ((store: TStore) => TStore);
